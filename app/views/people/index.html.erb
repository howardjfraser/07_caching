<% provide :nav, "#{link_to 'Derailed 00', root_path}".html_safe %>
<% provide :button, link_to('New Person', new_person_path, class: 'btn btn-success pull-xs-right') %>
<% provide :title, 'People' %>

<% @people.each do |p| %>
  <span class="p-r-1 nowrap">
    <%= p.name %>
    <%= link_to 'view', p %>
  </span>
<% end %>
